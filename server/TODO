Wishlist from Dennis to Oleg:

2. Add eMailAddress for schoolClass newsLetter on DB.
	Parameters from me: eMailAddress
	return: boolean
	
3. Remove eMailAddress from schoolClass newsLetter on DB.
	Parameters from me: eMailAddress
	return: boolean

4. Get complete newsLetterList for schoolClass.
	Parameters from me: schoolClass.
	return: newsletterList

5. Add email field in the database.

   Add method to get the emailAddress for a user.
	Parameters from me: userName
	return: emailAddress (String)
	
6. Delete operations for user & newsLetterEntries

7. Create a Cookie table in the database.
	(Only has a String field to store)
	
	Add a method to add a cookie in the database.
	Parameters from me: String (cookie)
	Return: boolean
	
	Add a method to remove a cookie from the database.
	Parameters from me: String (cookie)
	Return: boolean
	
	Add a method to search if a cookie exists in the database.
	Parameters from me: String (cookie)
	Return: boolean
	

General: Add forgot password function. (Auch im Pflichtenheft)