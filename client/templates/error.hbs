<div class="row">
	<div class="col-12">
		{{#with error}}
			<h2>{{no}} <small>{{title}}</small></h2>

			<p>{{message}}</p>
		{{/with}}

		<iframe width="100%" height="315" src="{{vidsource}}" frameborder="0" allowfullscreen></iframe>
	</div>
</div>
